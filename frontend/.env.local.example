# Copy to .env.local for local development.

# Server-only — deliberately NOT prefixed with NEXT_PUBLIC_, so it never
# reaches client-side code. Only Server Components and Route Handlers read
# this (see src/lib/api.ts).
PY_API_BASE_URL=http://localhost:8000
