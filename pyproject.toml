[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "injury-predictor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools]
packages = ["src"]
